app:
  github-token: "Token"
  stackoverflow:
    key: "Token"
    access-token: "Token"
  bot-url: http://localhost:8080/bot/api


spring:
  application:
    name: Scrapper
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  config:
    import: optional:file:.env[.properties]


#server:
#  port: 8081
