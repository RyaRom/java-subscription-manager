app:
  telegram-token: "TOKEN"
  scrapper-url: http://localhost:8081/scrapper/api

spring:
  application:
    name: Bot
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

server:
  port: 8080
